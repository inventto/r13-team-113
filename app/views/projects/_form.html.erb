<%= form_for @project, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= render :partial => 'image' %>
  </div>
  <div id="controls" class="control-group">
    <%= image_tag("uploadbutton.png", id: "upload-button") %>
    <%= image_tag("filterbutton.png", id: "filter-button") %>
    <%= image_tag("snapbutton.png", id: "snapshot-button") %>
    <%= image_tag("excldbutton.png", id: "excld-button") %>
    <%= image_tag("configbutton.png", id: "config-button") %>
  </div>
  <div id="context-container">
    <div id="config-context" style="display:none">
      <div class="control-group">
        <%= f.label :name, :class => 'control-label' %>
        <div class="controls">
          <%= f.text_field :name, :class => 'text_field' %>
        </div>
      </div>
      <div class="control-group">
        <%= f.label :url, :class => 'control-label' %>
        <div class="controls">
          <%= f.text_field :url, :class => 'text_field' %>
        </div>
      </div>
    </div>
  </div>
 
  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                projects_path, :class => 'btn' %>
  </div>
<% end %>
